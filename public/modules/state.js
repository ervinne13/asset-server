export const state = {
  currentPath: null,
  selectedFile: null,
  selectedFiles: new Set(),
  lastSelectedIdx: -1,
  currentItems: [],
  view: 'grid',
  sort: localStorage.getItem('sort') || 'newest',
  folderViews: {},
  scrollPositions: {},
  bookmarks: [],
  config: null,
  moveDestPath: null,
  photoMode: false,
};
